// Template Literal - uses back tick for print - similar to Fstring in Python
let pencilPrice = 20;
let penPrice = 40;

let totalPrice = `The total price is : ${penPrice + pencilPrice} BDT`;
console.log(totalPrice);

